#include "main.h"
/**
 * mul - multiplies two numbers together.
 * @a: The first number
 * @b: The second number
 * Return: the multiplication of two numbers.
 */
int mul(int a, int b)
{
	return (a * b);
}
